<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PayState">
	
	<select id="select" parameterType="int" resultType="PayState">
		select * from paystate where paystate_idx = #{paystate_idx}
	</select>
</mapper>